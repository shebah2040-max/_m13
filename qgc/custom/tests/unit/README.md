# Unit tests (Qt-based)

Placeholder for Qt Test-based unit suites for the Qt-bound layer (SafetyKernel,
AlertListModel, view-model bindings). Pure-C++ core tests live under
`tests/core/` instead. Populated in Pillar 5.
