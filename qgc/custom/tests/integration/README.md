# Integration tests

Placeholder for Qt-based integration tests that exercise the Safety Kernel
wrapper, MAVLink dialect handling, and FDR replay end-to-end. Will be
populated in Pillar 4/5. Until then, the directory exists so that
traceability entries referencing `tests/integration/` resolve in CI.
