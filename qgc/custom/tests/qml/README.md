# QML tests

Placeholder for Qt Quick Test suites that exercise the multi-console UI.
Added in Pillar 5 (Views Completion). Until then, the directory exists so
that traceability entries referencing `tests/qml/` resolve in CI.

See `docs/plans/SVP.md` for the planned verification approach.
